#!/usr/bin/make -f

# See debhelper(7) (uncomment to enable).
# Output every command that modifies files on the build system.
#export DH_VERBOSE = 1

%:
	dh $@ --with-python3

override_dh_install:
	dh_install -O--with-python3
	dh_python3
